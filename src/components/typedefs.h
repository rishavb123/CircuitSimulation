#ifndef TYPE_DEFS_H
#define TYPE_DEFS_H

#include <string>
#include <vector>
#include <unordered_map>

typedef std::unordered_map<std::string, std::vector<bool>> component_io_t;

#endif // !TYPE_DEFS_H